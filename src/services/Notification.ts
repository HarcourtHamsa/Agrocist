import {REGISTER_DEVICE_TOKEN} from 'utils/api/constant';
import networkRequest from 'utils/api/networkRequest';
import {displayToastMessage} from 'utils/display';

export const requestNotificationUserPermission = async () => {};

const getFCMNotification = async () => {};
